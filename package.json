{
  "name": "@koojy/jev-plugins",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Jev checks for personal development workflows",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "cli": "node --import tsx src/cli.ts",
    "test": "node --import tsx --test tests/*.test.ts plugins/*/tests/*.test.ts",
    "typecheck": "tsc --noEmit --module nodenext --moduleResolution nodenext --target es2022 --skipLibCheck --types node src/*.ts tests/*.ts plugins/*/src/*.ts plugins/*/tests/*.ts"
  },
  "dependencies": {
    "ai": "^7.0.105",
    "commander": "^15.0.0",
    "consola": "^3.4.2",
    "tsx": "^4.23.13",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^26.6.1",
    "typescript": "^7.0.2"
  }
}
